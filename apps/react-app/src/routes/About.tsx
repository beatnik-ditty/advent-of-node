import styled from '@emotion/styled';

const StyledAbout = styled.article({
  margin: '0.3em',
  maxWidth: '45em',
  color: '#cccccc',
});

export const About = () => (
  <div>
    <StyledAbout>
      <p>{ `// TODO` }</p>
    </StyledAbout>
  </div>
);
